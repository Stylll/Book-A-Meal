import { expect } from 'chai';
/**
 * Sample test suite
 */
/*
describe("Sample test to ensure test framework works", () => {
  it('should pass', () => {
    expect(1).to.equal(1);
  });

  it('should fail', () => {
    expect(2).to.equal(1);
  });
});
*/

